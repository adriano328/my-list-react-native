import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {},
    campo: {
        borderColor: '#33333',
        borderWidth: 2,
        width: '100%',
        borderRadius: 5,
        padding: 5,
        resizeMode: 'contain' 
    }
});